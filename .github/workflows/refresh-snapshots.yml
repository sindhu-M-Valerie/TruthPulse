name: Refresh Snapshot Data

on:
  schedule:
    # 00:30 UTC = 06:00 IST — runs daily at IST morning edition time
    - cron: '30 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate today's IST snapshot
        run: node generate-daily-snapshot.js

      - name: Commit snapshot updates
        run: |
          if git diff --quiet -- public/data; then
            echo "No snapshot changes"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/data
          git commit -m "chore: refresh snapshot data (IST daily edition)"
          git push
